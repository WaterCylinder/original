#ifndef MATHS_H
#define MATHS_H

namespace original{
    template<typename TYPE>
    TYPE abs(TYPE a);

    template<typename TYPE>
    TYPE max(TYPE a, TYPE b);

    template<typename TYPE>
    TYPE min(TYPE a, TYPE b);
}

#endif //MATHS_H
